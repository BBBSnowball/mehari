task clean(type: Exec) {
	commandLine "./pavement.py", "clean_results"

	environmentFromConfig()
}

task createIrFiles(type: Exec) {
	commandLine "./pavement.py", "create_ir_files"

	environmentFromConfig()

	mustRunAfter clean
}
